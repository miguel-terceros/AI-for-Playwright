## When generating scripts:

- always use the POM pattern
- use existing POM page files
- if something is missing in existing POM pages - update those first

Never look into app/ folder to view source code. Explore the app only through the browser.